import { makeStyles } from "@material-ui/styles";

export default makeStyles(
  () => ({
    content: {
      display: "flex", 
      alignItems: "center",
      justifyContent: "space-evenly",
      paddingBottom: 0
    }
  }),
  { index: 1 }
)